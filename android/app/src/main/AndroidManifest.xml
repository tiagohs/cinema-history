<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tiagohs.cinema_history">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <dist:module dist:instant="true" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:networkSecurityConfig="@xml/network_config_security">

        <activity
            android:name=".presentation.activities.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />
        <activity
            android:name=".presentation.activities.SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".presentation.activities.MainTopicsActivity" />
        <activity
            android:name=".presentation.activities.PresentationActivity"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />

        <activity android:name=".presentation.activities.MilMoviesPresentationActivity"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />

        <activity android:name=".presentation.activities.HistoryPagesActivity" />

        <activity android:name=".presentation.activities.PersonDetailsActivity"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />

        <activity android:name=".presentation.activities.MovieDetailsActivity"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />

        <activity android:name=".presentation.activities.PersonDetailsSpecialActivity"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />

        <activity android:name=".presentation.activities.TimelineActivity" />
        <activity android:name=".presentation.activities.SettingActivity" />
        <activity android:name=".presentation.activities.AboutActivty"
            android:theme="@style/AboutTheme"/>
        <activity android:name=".presentation.activities.ReferenceActivity" />



        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.tiagohs.cinema_history.helpers.tools.CastOptionsProvider" />

        <!--        <activity android:name=".ui.activities.PresentationActivity" />-->
    </application>

</manifest>